///
/// HybridAudioPlayerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `PlayerConfig` to properly resolve imports.
namespace margelo::nitro::margelo::audiowaveform { struct PlayerConfig; }
// Forward declaration of `DurationType` to properly resolve imports.
namespace margelo::nitro::margelo::audiowaveform { enum class DurationType; }

#include <NitroModules/Promise.hpp>
#include "PlayerConfig.hpp"
#include "DurationType.hpp"
#include <functional>

namespace margelo::nitro::margelo::audiowaveform {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `AudioPlayer`
   * Inherit this class to create instances of `HybridAudioPlayerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridAudioPlayer: public HybridAudioPlayerSpec {
   * public:
   *   HybridAudioPlayer(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridAudioPlayerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridAudioPlayerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridAudioPlayerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> prepare(const PlayerConfig& config) = 0;
      virtual std::shared_ptr<Promise<bool>> start(double finishMode, double speed) = 0;
      virtual std::shared_ptr<Promise<bool>> pause() = 0;
      virtual std::shared_ptr<Promise<bool>> stop() = 0;
      virtual std::shared_ptr<Promise<bool>> seekTo(double position) = 0;
      virtual std::shared_ptr<Promise<bool>> setVolume(double volume) = 0;
      virtual std::shared_ptr<Promise<bool>> setPlaybackSpeed(double speed) = 0;
      virtual std::shared_ptr<Promise<double>> getDuration(DurationType type) = 0;
      virtual std::shared_ptr<Promise<double>> getCurrentPosition() = 0;
      virtual bool isPlaying() = 0;
      virtual void onPlaybackUpdate(const std::function<void(double /* position */)>& callback) = 0;
      virtual void onPlaybackFinished(const std::function<void()>& callback) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "AudioPlayer";
  };

} // namespace margelo::nitro::margelo::audiowaveform
