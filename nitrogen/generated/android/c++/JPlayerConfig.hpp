///
/// JPlayerConfig.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "PlayerConfig.hpp"

#include "JUpdateFrequency.hpp"
#include "UpdateFrequency.hpp"
#include <optional>
#include <string>

namespace margelo::nitro::margelo::audiowaveform {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "PlayerConfig" and the the Kotlin data class "PlayerConfig".
   */
  struct JPlayerConfig final: public jni::JavaClass<JPlayerConfig> {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/com/audiowaveform/PlayerConfig;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct PlayerConfig by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    PlayerConfig toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldPath = clazz->getField<jni::JString>("path");
      jni::local_ref<jni::JString> path = this->getFieldValue(fieldPath);
      static const auto fieldVolume = clazz->getField<jni::JDouble>("volume");
      jni::local_ref<jni::JDouble> volume = this->getFieldValue(fieldVolume);
      static const auto fieldUpdateFrequency = clazz->getField<JUpdateFrequency>("updateFrequency");
      jni::local_ref<JUpdateFrequency> updateFrequency = this->getFieldValue(fieldUpdateFrequency);
      static const auto fieldStartPosition = clazz->getField<jni::JDouble>("startPosition");
      jni::local_ref<jni::JDouble> startPosition = this->getFieldValue(fieldStartPosition);
      return PlayerConfig(
        path->toStdString(),
        volume != nullptr ? std::make_optional(volume->value()) : std::nullopt,
        updateFrequency != nullptr ? std::make_optional(updateFrequency->toCpp()) : std::nullopt,
        startPosition != nullptr ? std::make_optional(startPosition->value()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JPlayerConfig::javaobject> fromCpp(const PlayerConfig& value) {
      using JSignature = JPlayerConfig(jni::alias_ref<jni::JString>, jni::alias_ref<jni::JDouble>, jni::alias_ref<JUpdateFrequency>, jni::alias_ref<jni::JDouble>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        jni::make_jstring(value.path),
        value.volume.has_value() ? jni::JDouble::valueOf(value.volume.value()) : nullptr,
        value.updateFrequency.has_value() ? JUpdateFrequency::fromCpp(value.updateFrequency.value()) : nullptr,
        value.startPosition.has_value() ? jni::JDouble::valueOf(value.startPosition.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::margelo::audiowaveform
